import React from "react";
import { BrowserRouter } from "react-router-dom";
import Router from "core/router";

function App() {
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  );
}

export default App;
